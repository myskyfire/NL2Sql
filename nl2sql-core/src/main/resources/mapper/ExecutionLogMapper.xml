<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.nl2sql.core.mapper.ExecutionLogMapper">
    
    <!-- 记录SQL执行日志 -->
    <insert id="insertExecutionLog">
        INSERT INTO sql_execution_logs 
        (user_id, username, sql_text, execution_time_ms, row_count, 
         is_slow_query, status, error_message, ip_address)
        VALUES (#{userId}, #{username}, #{sqlText}, #{executionTimeMs}, #{rowCount}, 
                #{isSlowQuery}, #{status}, #{errorMessage}, #{ipAddress})
    </insert>
    
</mapper>
