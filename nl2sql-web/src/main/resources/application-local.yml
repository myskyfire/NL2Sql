# 本地开发配置
# 
# 注意：此文件已被 .gitignore 忽略，不会提交到 Git
# 请在此填入真实的 API Key

llm:
  active-provider: aliyun  # ← 使用阿里云 Provider
  aliyun:
    enabled: true  # ← 启用阿里云 Provider
    api-key: sk-3c02e4dfe6674b3cbace729e2ea4ac7b  # ← 替换为你的真实阿里云 API Key
    model: qwen3.5-122b-a10b  # ← 在这里切换模型：qwen-plus / qwen3.5-plus / qwen-max / qwen-turbo
    timeout: 120  # ← 超时时间（秒），配合重试机制（60s → 90s）

# 管理端点 API Key（可选，用于保护 /api/admin/* 接口）
admin:
  api:
    key:

# Tracing 配置（本地开发，不可提交到 Git）
tracing:
  provider: langsmith
  langsmith:
    enabled: true
    api-key: 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
