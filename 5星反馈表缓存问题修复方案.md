# 5星反馈表缓存问题 - 根本原因与修复方案

## 🔍 根本原因分析

### **问题现象**
用户查询"昨天王五的订单"并给5星反馈后，下次查询"前天赵六的订单列表"时，L3缓存虽然命中（Chroma 0.940分），但返回的表列表不完整。

### **根因定位**

1. **`nl2sql_query_log`表缺少`selected_tables`字段**
   - 建表SQL（`create_query_log_table.sql`）中只有基础字段
   - `SQLFeedbackService`从日志查询`selected_tables`时返回NULL
   - 降级到从SQL提取表，但LLM生成的SQL可能只用了部分表

2. **AgentChatService没有写入表列表**
   - `logQueryToDatabase()`方法插入日志时，`selected_tables`字段为NULL
   - 导致后续5星反馈无法获取完整的表列表

3. **StandardQuerySkill没有传递表列表**
   - NL2SQLService.generateSQL()只返回SQL字符串
   - 表列表在TableSelectionOrchestrator中被丢弃

---

## ✅ 修复方案

### **方案架构**
```
NL2SQLService (保存表列表到SessionContextManager)
    ↓
AgentChatService.enrichResponse (从SessionContext读取并放入response)
    ↓
AgentChatService.logQueryToDatabase (从response读取并写入nl2sql_query_log.selected_tables)
    ↓
SQLFeedbackService (从nl2sql_query_log读取selected_tables用于5星反馈缓存)
```

### **修改文件清单**

#### **1. 数据库DDL（需手动执行）**
- 文件：`add_selected_tables_column.sql`
- 操作：ALTER TABLE nl2sql_query_log ADD COLUMN selected_tables TEXT

#### **2. SessionContextManager.java**
- 新增：`SESSION_TABLES_MAP` ConcurrentHashMap存储表列表
- 新增：`saveSelectedTables(List<String>)` 方法
- 新增：`getSelectedTables()` 方法

#### **3. NL2SQLService.java**
- 修改：在`generateSQLWithTables()`末尾调用`sessionContextManager.saveSelectedTables(allTables)`
- 目的：将LLM选择的完整表列表保存到会话上下文

#### **4. AgentChatService.java**
- 修改：`enrichResponse()`方法，从SessionContextManager读取表列表并放入response
- 修改：`logQueryToDatabase()`方法，从response读取`selectedTables`并写入数据库
- 新增：INSERT语句包含`selected_tables`字段

#### **5. SQLFeedbackRequest.java**（已完成）
- 新增：`usedTables`字段，支持显式传入表列表

#### **6. SQLFeedbackService.java**（已完成）
- 修改：三级优先级获取表列表
  1. 优先使用传入的`usedTables`
  2. 降级使用`nl2sql_query_log.selected_tables`
  3. 保底从SQL提取

---

## 📋 执行步骤

### **步骤1：执行DDL添加字段**
```bash
mysql -h 192.168.31.200 -P 3306 -u root -pQq123456. nl2sql_meta_db < add_selected_tables_column.sql
```

验证：
```sql
DESCRIBE nl2sql_query_log;
-- 应该看到 selected_tables TEXT 字段
```

### **步骤2：重新编译打包**
```powershell
cd "D:\WorkSpace\idea workspace\NL2Sql"
mvn clean package -DskipTests
```

### **步骤3：重启应用**
```powershell
.\start.ps1
```

### **步骤4：验证修复效果**

#### **测试场景1：5星反馈学习**
1. 查询："昨天张三的订单详情"
2. 观察日志：
   ```
   [NL2SQLService] 已保存表列表: sessionId=xxx, tables=[orders, users, products, order_items, ...]
   [enrichResponse] 已添加 selectedTables: [orders, users, products, order_items, ...]
   [查询日志] 记录成功: sessionId=xxx, success=true
   ```
3. 检查数据库：
   ```sql
   SELECT session_id, question, selected_tables FROM nl2sql_query_log ORDER BY created_at DESC LIMIT 1;
   -- 应该看到 selected_tables 字段有值，如 ["orders","users","products",...]
   ```
4. 给5星反馈
5. 查询："前天李四的订单列表"
6. 观察日志：
   ```
   [表缓存注入] ✅ 从日志获取表列表: [orders, users, products, order_items, ...]
   [MetadataCache] ✅ L3语义匹配成功(Chroma): query='{PERSON}的订单列表', tables=[完整的表列表]
   ```

#### **测试场景2：归一化缓存命中**
1. 第一次查询："昨天张三的订单" → 5星
2. 第二次查询："前天李四的订单" → 应命中L3缓存
3. 观察日志：
   ```
   SchemaRetrievalService.normalizeQueryForCache: '昨天张三的订单' -> '{DATE_RELATIVE}{PERSON}的订单'
   SchemaRetrievalService.normalizeQueryForCache: '前天李四的订单' -> '{DATE_RELATIVE}{PERSON}的订单'
   [MetadataCache] ✅ L3语义匹配成功(Chroma): query='{PERSON}的订单', similarity=0.9xx
   ```

---

## ⚠️ 注意事项

1. **必须执行DDL**：否则`INSERT INTO nl2sql_query_log`会报错
2. **ThreadLocal清理**：SessionContextManager使用ConcurrentHashMap，需要定期清理过期sessionId（当前未实现，可能导致内存泄漏）
3. **兼容性**：如果`selected_tables`为NULL，SQLFeedbackService会降级到从SQL提取，不影响现有功能

---

## 🎯 预期效果

修复后，5星反馈时将缓存**LLM实际选择的完整表列表**，而非从SQL中提取的不完整表列表。下次相似查询时，能召回所有必要的表，避免因表缺失导致的SQL生成失败。
