# NL2SQL 用户查询规范指南

## 📖 文档说明

本文档旨在帮助你更高效地使用 NL2SQL 系统,通过规范的查询方式获得更准确的 SQL 生成结果。

**核心原则**: **越规范的输入 = 越准确的输出**

---

## 🎯 一、查询基本原则

### 1.1 明确业务实体

✅ **推荐写法**:
```
查询张三在2024年的订单总金额
查看北京地区的销售数据
统计华东区所有客户的订单数量
```

❌ **避免写法**:
```
查一下那个人的订单
看看那边的销售情况
统计一下客户数据
```

**原因**: 系统通过 HanLP NER 识别具体的人名、地名实体,模糊指代无法准确映射到数据库字段。

---

### 1.2 使用标准时间表达

✅ **推荐写法**:
```
2024年1月的订单
最近30天的销售数据
2023年第一季度利润
上个月的用户注册数
```

❌ **避免写法**:
```
前阵子的订单
前些日子的销售
去年的某个时候
```

**原因**: 系统支持的时间表达式包括:
- 绝对时间: `2024年1月`、`2023-01-01`
- 相对时间: `最近7天`、`上个月`、`今年`
- 时间范围: `2024年1月到3月`、`Q1`

---

### 1.3 明确聚合维度

✅ **推荐写法**:
```
按月份统计订单数量
每个地区的销售总额
各产品类别的平均价格
```

❌ **避免写法**:
```
统计一下订单
看看销售情况
分析一下价格
```

**原因**: 明确的聚合维度(按什么分组)能帮助系统生成正确的 GROUP BY 语句。

---

## 🏢 二、行业术语规范

### 2.1 财务领域术语对照

| 业务说法 | 系统理解 | 对应字段/表 |
|---------|---------|------------|
| 营收/收入 | revenue | orders.total_amount |
| 利润 | profit | orders.profit |
| 成本 | cost | orders.cost |
| 毛利率 | gross_margin | (revenue-cost)/revenue |
| 净利润 | net_profit | orders.net_profit |
| 应收账款 | receivables | finance_receivables.amount |
| 应付账款 | payables | finance_payables.amount |

**示例**:
```
✅ 查询2024年各月份的营收和利润
✅ 统计华东区的毛利率排名
❌ 查一下赚了多少钱
```

---

### 2.2 贸易领域术语对照

| 业务说法 | 系统理解 | 对应字段/表 |
|---------|---------|------------|
| 出口额 | export_amount | trade_orders.export_value |
| 进口额 | import_amount | trade_orders.import_value |
| 贸易顺差 | trade_surplus | export - import |
| 贸易逆差 | trade_deficit | import - export |
| 海关编码 | hs_code | trade_products.hs_code |
| 报关单 | customs_declaration | trade_customs.declaration_no |

**示例**:
```
✅ 查询2024年对美国的出口额
✅ 统计HS编码85章的进口商品Top10
❌ 看看外贸情况
```

---

### 2.3 通用业务术语

| 业务说法 | 系统理解 |
|---------|---------|
| Top10/前10名 | ORDER BY xxx DESC LIMIT 10 |
| 排名/排行 | RANK() OVER (ORDER BY xxx) |
| 同比 | 与去年同期对比 |
| 环比 | 与上期(上月/上周)对比 |
| 占比/比例 | xxx / SUM(xxx) * 100% |
| 平均值 | AVG() |
| 最大值/最小值 | MAX()/MIN() |

---

## 🔍 三、查询模式模板

### 3.1 简单查询模式

**模板**: `[时间] + [主体] + [指标]`

```
示例:
- 2024年订单总数
- 张三的订单金额
- 北京地区的客户数量
```

---

### 3.2 分组统计模式

**模板**: `按[维度]统计[时间]的[指标]`

```
示例:
- 按月份统计2024年的订单数量
- 按地区统计上个月的销售额
- 按产品类别统计今年的利润
```

---

### 3.3 排序筛选模式

**模板**: `[时间] + [主体] + [指标] + TopN/排名`

```
示例:
- 2024年销售额Top10的产品
- 上个月订单量最多的5个地区
- 今年利润排名前20的客户
```

---

### 3.4 对比分析模式

**模板**: `[指标] + 同比/环比 + [时间]`

```
示例:
- 2024年营收同比增长率
- 本月销售额环比变化
- 各地区利润占比分布
```

---

### 3.5 多条件筛选模式

**模板**: `[时间] + [条件1] + [条件2] + [指标]`

```
示例:
- 2024年华东区销售额大于10万的订单
- 上个月北京地区张三的订单明细
- 今年电子产品类别且库存大于100的商品
```

---

## ⚠️ 四、常见错误与修正

### 错误1: 缺少时间范围

❌ `查询订单总额`  
✅ `查询2024年订单总额`  
✅ `查询所有时间的订单总额`(如需全量)

**原因**: 系统默认会询问时间范围,明确时间可加速查询。

---

### 错误2: 歧义指标名称

❌ `查一下收入`  
✅ `查询2024年营收(revenue)`  
✅ `查询2024年净利润`

**原因**: "收入"可能指营收、净利润、毛利润等,需明确具体指标。

---

### 错误3: 嵌套复杂逻辑

❌ `查询那些买了A产品又买了B产品但没买C产品的客户中,消费最高的前10个`  
✅ 分步查询:
  1. `查询同时购买A产品和B产品的客户`
  2. `从上述客户中排除购买过C产品的`
  3. `按消费金额排序取Top10`

**原因**: 过于复杂的嵌套逻辑容易出错,建议拆分为多轮对话。

---

### 错误4: 使用口语化表达

❌ `帮我看看去年卖得咋样`  
✅ `查询2023年销售总额和订单数量`  
✅ `查询2023年各月份的销售趋势`

**原因**: 口语化表达缺乏明确的指标和维度。

---

### 错误5: 忽略数据源限定

❌ `查询订单数据`  
✅ `查询财务系统的订单数据`  
✅ `查询贸易数据库的出口订单`

**原因**: 多数据源场景下,明确数据源可避免表混淆。

---

## 💡 五、高级技巧

### 5.1 利用历史反馈

如果某次查询结果不准确:
1. 给出 **1-2分低评分**
2. 在反馈框说明期望结果
3. 系统会将此案例加入训练集
4. 下次类似查询会更准确

**示例反馈**:
```
评分: 2分
反馈: 我想看的是"净利润",不是"营收"。净利润 = 营收 - 成本 - 税费
```

---

### 5.2 使用行业限定词提升精度

在查询前加上行业前缀:

```
✅ 【财务】查询2024年Q1的毛利率
✅ 【贸易】查询对欧盟的出口额
✅ 【人事】查询各部门员工数量
```

**原因**: 行业限定词会激活对应的术语映射规则。

---

### 5.3 多轮对话澄清

第一轮: `查询销售数据`  
系统: `请问您要查询哪个时间范围?哪些指标?`  
第二轮: `2024年按月统计的销售额和订单数`

**优势**: 逐步澄清比一次性复杂描述更可靠。

---

### 5.4 参考成功案例

在聊天界面点击 **"相似案例"** 按钮,查看历史上成功的查询示例,模仿其表达方式。

---

## 📊 六、查询效果监控

你可以通过以下API查看系统运行状态(需管理员权限):

### 6.1 归一化效果统计
```
GET /api/monitor/normalization-stats
```
查看人名/地名识别率,判断是否需要补充实体词典。

### 6.2 缓存命中率
```
GET /api/monitor/cache-hit-rate
```
查看L1/L2/L3缓存命中情况,高命中率意味着相似查询复用效果好。

### 6.3 低分案例分析
```
GET /api/monitor/low-rating-analysis
```
查看最近低分反馈,针对性优化查询方式。

---

## 🔄 七、持续优化机制

### 7.1 系统如何学习

1. **自动收集**: 所有查询记录存入 `nl2sql_query_log`
2. **用户反馈**: 1-5分评分 + 文字反馈
3. **定期训练**: 
   - 高分案例(rating≥4) → 加入RAG示例库
   - 低分案例(rating≤2) → 人工标注后加入训练集
4. **规则更新**: 根据常见问题补充正则表达式和术语映射

### 7.2 你可以做什么

- ✅ **积极评分**: 每次查询后给出真实评分
- ✅ **详细反馈**: 低分时说明期望结果
- ✅ **规范表达**: 遵循本文档的查询规范
- ✅ **报告问题**: 发现系统性错误及时反馈

---

## ❓ 八、常见问题 FAQ

### Q1: 为什么同样的查询有时准有时不准?
**A**: 可能原因:
- LLM生成的随机性(温度参数影响)
- 上下文对话历史干扰
- 缓存命中了不同版本的SQL

**解决**: 
- 开启新会话重新查询
- 使用更规范的表达
- 检查是否有歧义

---

### Q2: 如何提高复杂查询的准确率?
**A**: 
1. 拆分为多个简单查询
2. 使用多轮对话逐步构建
3. 明确每个步骤的中间结果
4. 必要时手动调整生成的SQL

---

### Q3: 系统支持哪些数据库?
**A**: 当前支持:
- MySQL 5.7+
- PostgreSQL 12+
- Oracle 11g+(需配置驱动)

扩展其他数据库需修改元数据适配层。

---

### Q4: 能否查询跨库关联的数据?
**A**: 当前不支持跨库JOIN。  
** workaround**: 
- 分别查询两个库的数据
- 在应用层进行数据合并
- 或建立ETL将数据同步到同一库

---

### Q5: 查询速度慢怎么办?
**A**: 优化建议:
1. 缩小时间范围(如从"全年"改为"单月")
2. 减少返回字段(只查需要的列)
3. 添加筛选条件减少数据量
4. 检查是否命中缓存(L1最快)

---

## 📞 九、技术支持

如遇问题,请提供以下信息:
1. 查询语句原文
2. 生成的SQL(如有)
3. 期望结果 vs 实际结果
4. 评分和反馈内容
5. 截图(如有)

---

## 📝 附录: 快速参考卡片

```
┌─────────────────────────────────────────────┐
│         NL2SQL 查询速查表                     │
├─────────────────────────────────────────────┤
│ ✅ 要做:                                     │
│ • 明确时间范围                               │
│ • 使用具体人名/地名                          │
│ • 指定聚合维度(按什么分组)                    │
│ • 使用标准术语(营收/利润/成本)                │
│ • 复杂查询分步进行                           │
│                                             │
│ ❌ 避免:                                     │
│ • 模糊指代(那个人/那边/前阵子)                │
│ • 缺少时间限定                               │
│ • 嵌套超过3层的复杂逻辑                       │
│ • 口语化表达(卖得咋样/赚了多少)               │
│ • 一次性描述过多条件                         │
└─────────────────────────────────────────────┘
```

---

**文档版本**: v1.0  
**最后更新**: 2026-04-25  
**维护者**: NL2SQL 项目组
