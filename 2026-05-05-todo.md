# NL2SQL 系统重构后问题清单

> 扫描时间: 2026-05-08
> 编译状态: ✅ 通过 (mvn compile)

---

## 🔴 P0 - 严重问题（需立即修复）

### 1. API Key 硬编码在配置文件中

**文件**: `nl2sql-web/src/main/resources/application.yml:166`

Jina API Key 有硬编码默认值：
```yaml
api-key: ${JINA_API_KEY:jina_8dbdaf5c1b7d40dca36fae57943eec15EjCz5qiHqCfdJl2x7q9vfLZm84hJ}
```

**文件**: `nl2sql-web/src/main/resources/application-local.yml:10`

阿里云 API Key 硬编码：
```yaml
api-key: sk-3c02e4dfe6674b3cbace729e2ea4ac7b
```

**风险**: API Key 泄露到代码仓库，任何有代码访问权限的人都能获取。

**修复方案**: 移除默认值，仅使用环境变量；application-local.yml 加入 .gitignore。

---

### 2. SkillsMetadataLoader 与 GroovySkillExecutor 技能目录不一致

**文件**: `nl2sql-core/.../skills/SkillsMetadataLoader.java:67`

SkillsMetadataLoader 硬编码了2个技能目录：
```java
String[] skillDirs = {"standard-query", "report-with-insights"};
```

**文件**: `nl2sql-core/.../skills/GroovySkillExecutor.java:38`

GroovySkillExecutor 通过配置文件读取5个目录：
```java
@Value("${skills.groovy.scan-dirs:}")
private List<String> scanDirs = Collections.emptyList();
```

**文件**: `nl2sql-web/src/main/resources/application.yml`

配置了5个目录：
```yaml
skills:
  groovy:
    scan-dirs:
      - standard-query
      - report-with-insights
      - sql-validate-execute
      - sql-performance-analysis
      - hybrid-example
```

**影响**: SkillsMetadataLoader 只加载2个技能元数据，GroovySkillExecutor 加载5个，LLM 看到的 Skills 描述和实际可用的 Skills 不匹配。

**修复方案**: SkillsMetadataLoader 也从 `skills.groovy.scan-dirs` 配置读取目录列表，与 GroovySkillExecutor 保持一致。

---

## 🟠 P1 - 重要问题（应尽快修复）

### 3. 废弃代码未清理

- `nl2sql-web/.../service/NL2SQLDepService.java` - 整个类标记为 `@Deprecated`，方法直接抛异常，但仍被 NL2SQLController 注入使用
- `nl2sql-web/.../controller/NL2SQLController.java` - `/api/query` 和 `/api/correct` 端点已废弃，但仍注入了10个依赖（包括 NL2SQLDepService），浪费资源

**修复方案**:
- 移除 NL2SQLDepService 类
- NL2SQLController 中移除对 NL2SQLDepService 的依赖，保留仍有用的端点（export/excel、manual-sql）
- 已废弃端点标记为 `@Deprecated` 并在注释中指明替代端点

---

### 4. 大量 @Autowired(required = false) 滥用

全项目共发现 **100处** `@Autowired(required = false)`，集中在：

| 文件 | 数量 |
|------|------|
| AgentConfig.java | 10 |
| AgentChatService.java | 8 |
| SQLFeedbackService.java | 5 |
| FeedbackLearningService.java | 5 |
| SqlWorker.java | 4 |
| NL2SQLService.java | 4 |
| TableSelectionOrchestrator.java | 4 |
| SynonymService.java | 4 |
| IndustryConceptDictionary.java | 3 |
| 其他 | 53 |

**风险**: 隐藏了依赖缺失问题，运行时可能因 NPE 导致难以排查的 Bug。

**修复方案**:
- 核心依赖改为构造器注入（required=true）
- 真正可选的依赖保留 @Autowired(required=false)，但必须在使用前做 null 检查
- 逐步迁移到构造器注入模式

---

### 5. ObjectMapper 实例大量重复创建

全项目发现 **54处** `new ObjectMapper()`，每个类各自创建实例。

**问题**: ObjectMapper 是线程安全且重量级的对象，应复用 Spring 容器中的单例。项目中已有 `JsonUtils.java` 提供了静态 ObjectMapper。

**修复方案**:
- Spring 管理的 Bean 中注入 Spring 容器的 ObjectMapper
- 非 Spring 管理的类使用 `JsonUtils.getObjectMapper()`
- 逐步替换各处 `new ObjectMapper()`

---

### 6. StreamChatService 多处 TODO 未实现

**文件**: `nl2sql-web/.../service/StreamChatService.java`

| 行号 | TODO 内容 | 影响 |
|------|-----------|------|
| 99 | 保存用户消息 - 待实现 | 对话历史缺失 |
| 102 | 获取对话历史 - 待实现 | LLM 无多轮上下文 |
| 116 | 传入历史消息 - 传 null | LLM 无多轮上下文 |
| 229 | 保存AI回复 - 待实现 | 对话历史缺失 |
| 241 | 发布澄清事件 - 待实现 | 澄清流程不完整 |

**影响**: 流式对话功能缺少对话历史上下文，LLM 无法利用多轮对话信息。

**修复方案**: 集成 ConversationHistoryService 实现消息持久化和历史加载。

---

## 🟡 P2 - 一般问题（建议修复）

### 7. Groovy 脚本使用 println 而非日志框架

4个 Groovy 脚本中共 **24处** `println`：

| 文件 | 数量 |
|------|------|
| StandardQuerySkill.groovy | 17 |
| SQLPerformanceAnalysisSkill.groovy | 4 |
| SQLValidateAndExecuteSkill.groovy | 3 |
| SimpleDataQuerySkill.groovy | 5 |

**问题**: println 输出无法被日志框架收集，生产环境无法控制日志级别和输出目标。

**修复方案**: 在 SkillContext 中提供日志工具方法，Groovy 脚本通过 context.log() 输出日志。

---

### 8. 空壳 Provider 实现（QdrantVectorProvider / MilvusVectorProvider）

**文件**:
- `nl2sql-core/.../rag/provider/QdrantVectorProvider.java`
- `nl2sql-core/.../rag/provider/MilvusVectorProvider.java`

所有方法都是空实现（仅含 TODO 注释），但 `available` 被设为 `true`，可能误导调用方认为服务可用。

**修复方案**: 将 `available` 默认设为 `false`，或在初始化时打印明确的 "未实现" 警告日志。

---

### 9. TemplateController 完全是空壳

**文件**: `nl2sql-web/.../controller/TemplateController.java`

所有4个接口都返回空数据或 "TODO"，前端模板管理页面形同虚设。

**修复方案**: 如果短期不实现，应在接口文档中标注为 "规划中"，或暂时移除该 Controller 和对应的前端页面。

---

### 10. @CrossOrigin(origins = "*") 安全风险

**文件**:
- `nl2sql-web/.../controller/AuthController.java:18`
- `nl2sql-web/.../controller/NL2SQLController.java:29`

允许任何域名的跨域请求，存在 CSRF 攻击风险。

**修复方案**: 配置全局 CORS 策略，限制允许的域名列表。

---

### 11. 生产环境日志级别过高

**文件**: `nl2sql-web/src/main/resources/application.yml`

```yaml
logging:
  level:
    com.nl2sql: DEBUG
    dev.langchain4j: DEBUG
    org.springframework.jdbc.core: DEBUG
```

**问题**: 所有包都设为 DEBUG 级别，生产环境会产生大量日志，影响性能和磁盘空间。

**修复方案**: 主配置设为 INFO，开发环境通过 application-local.yml 覆盖为 DEBUG。

---

### 12. 数据库密码硬编码

**文件**: `nl2sql-web/src/main/resources/application.yml:27`

```yaml
password: 123456
```

**修复方案**: 使用环境变量 `${DB_PASSWORD:123456}` 替代，生产环境通过环境变量注入真实密码。

---

## 📊 问题统计

| 优先级 | 数量 | 类别 |
|--------|------|------|
| P0 严重 | 2 | 安全漏洞、数据不一致 |
| P1 重要 | 4 | 废弃代码、依赖注入、资源浪费、功能缺失 |
| P2 一般 | 6 | 代码规范、空壳实现、安全配置 |
| **合计** | **12** | |

---

## 🔧 建议修复顺序

1. **P0-1**: 移除 API Key 硬编码（安全优先）
2. **P0-2**: 统一 SkillsMetadataLoader 与 GroovySkillExecutor 的技能目录（功能正确性）
3. **P1-3**: 清理废弃代码 NL2SQLDepService / NL2SQLController
4. **P1-6**: 实现 StreamChatService 的对话历史功能
5. **P1-5**: 统一 ObjectMapper 使用
6. **P1-4**: 逐步减少 @Autowired(required=false) 滥用
7. **P2 问题**: 按需修复
