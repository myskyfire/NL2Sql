<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.nl2sql.auth.mapper.AuthMapper">
    
    <!-- 检查用户是否存在 -->
    <select id="checkUserExists" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM users WHERE id = #{userId}
    </select>
    
    <!-- 添加白名单（存在则更新） -->
    <insert id="insertOrUpdateWhitelist">
        INSERT INTO whitelist (user_id, added_by, reason, expires_at, is_active)
        VALUES (#{userId}, #{addedBy}, #{reason}, #{expiresAt}, 1)
        ON DUPLICATE KEY UPDATE
            added_by = VALUES(added_by),
            reason = VALUES(reason),
            expires_at = VALUES(expires_at),
            is_active = 1
    </insert>
    
    <!-- 撤销表权限 -->
    <update id="revokeTablePermission">
        UPDATE table_permissions 
        SET is_active = 0 
        WHERE user_id = #{userId} 
          AND table_name = #{tableName}
    </update>
    
    <!-- 停用白名单 -->
    <update id="deactivateWhitelist">
        UPDATE whitelist 
        SET is_active = 0 
        WHERE user_id = #{userId}
    </update>
    
    <!-- 获取用户名 -->
    <select id="getUsernameById" resultType="java.lang.String">
        SELECT username FROM users WHERE id = #{userId}
    </select>
    
</mapper>
