<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.nl2sql.core.rag.mapper.RagKnowledgeMapper">
    
    <!-- 插入RAG知识 -->
    <insert id="insertRagKnowledge">
        INSERT INTO rag_knowledge (question, answer, sql_example, category, created_at)
        VALUES (#{question}, #{answer}, #{sqlExample}, #{category}, NOW())
    </insert>
    
    <!-- 查询相似知识(全文搜索) -->
    <select id="findSimilarKnowledge" resultType="java.util.HashMap">
        SELECT question, answer, sql_example, category
        FROM rag_knowledge
        WHERE MATCH(question) AGAINST(#{question} IN NATURAL LANGUAGE MODE)
        ORDER BY created_at DESC
        LIMIT #{limit}
    </select>
    
</mapper>
