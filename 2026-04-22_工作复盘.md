# 2026-04-22 工作复盘

**会话时间**: 2026-04-22 07:49 - 18:00 (约10小时)  
**提交Commit**: `feat: add metadata auto-enhancement with LLM`

## 一、今日工作内容

### 1.1 SYSTEM_INTRODUCTION.md文档评估与改进方案

**背景**: Trae生成的系统总结文档指出现有架构存在多个问题,需要验证并制定改进方案。

**问题验证结果**:
- ✅ **确认存在的问题**:
  1. Groovy Skills是"伪Skill" - 直接调用Bean而非Tool链
  2. WorkflowEngine已实现但未启用(缺少Workflow配置)
  3. SQLCorrectionService未被Skills复用(代码重复)
  4. generateWithTools模型选择存疑(推理模型vs代码模型)
  
- ⚠️ **部分不准确**:
  - "双模型路由未完全启用"描述有误,实际已基本正确配置

**改进方案分级**:
- P0: 统一SQL纠错机制 + 补充核心单元测试
- P1: 创建Workflow原子Tools + 启用WorkflowEngine试点 + 明确模型策略
- P2: 更新系统文档

---

### 1.2 Agent架构优化任务执行

#### (1) 统一SQL纠错机制 (P0 - 已完成)
**修改文件**: `StandardQuerySkill.groovy` (第749-828行)

**改动内容**:
```groovy
// 优先使用SQLCorrectionService
SQLCorrectionService correctionService = context.getBean(SQLCorrectionService.class)
SQLCorrectionService.CorrectionResult result = 
    correctionService.autoCorrect(currentSql, error, maxRetries)

if (result.success) {
    return sqlExecutionTool.executeSQL(result.correctedSQL, ...)
} else {
    // 降级为原有LLM修正逻辑
    return executeWithAutoFix(...)
}
```

**影响点分析**:
- ✅ 消除代码重复,集中管理纠错规则
- ⚠️ Groovy中调用Java Service增加~5ms反射开销
- 🔍 复杂JOIN语句的歧义字段修正可能不准确

---

#### (2) 补充核心单元测试 (P0 - 已完成)
**新增测试文件**:
1. `ReActAgentTest.java` - 7个测试用例
   - Tool注册验证
   - 执行循环测试
   - 结构化数据检测
   - 最大迭代次数限制
   
2. `SQLCorrectionServiceTest.java` - 8个测试用例
   - 语法错误修正(多余分号)
   - 括号匹配补全
   - 字段拼写纠正
   - LLM降级机制
   - 最大重试次数

**测试结果**: mvn test 全部通过 ✅

---

#### (3) 创建Workflow原子Tools (P1 - 已完成)
**新增3个Tool类**:

1. **RetrieveTableSchemaTool.java**
   - 功能: 包装NL2SQLTool.retrieveSchema()
   - 职责: 根据问题检索相关表结构
   - 标注: `[Workflow]`

2. **GenerateSQLFromSchemaTool.java**
   - 功能: 包装NL2SQLTool.generateSQL() + 澄清检测
   - 职责: 基于表结构生成SQL
   - 标注: `[Workflow]`

3. **ExecuteSafeSQLTool.java**
   - 功能: 包装SQLExecutionTool.executeSQL()
   - 职责: 执行经过验证的SQL查询
   - 标注: `[Workflow]`

**注册位置**: `AgentConfig.java` (第614-645行)

---

#### (4) 启用WorkflowEngine试点 (P1 - 已完成)
**新增**: `simple-data-query/SKILL.md`

**Workflow配置**:
```yaml
workflow:
  steps:
    - id: retrieve_schema
      action: call_tool
      tool: retrieve_table_schema
      output_var: schema
    
    - id: generate_sql
      action: call_tool
      tool: generate_sql_from_schema
      input:
        schema: "{{schema}}"
      output_var: sql
    
    - id: validate_sql
      action: call_tool
      tool: validate_sql
      input:
        sql: "{{sql}}"
    
    - id: execute_query
      action: call_tool
      tool: execute_safe_sql
      input:
        sql: "{{sql}}"
```

**修改**: `AgentConfig.java` 支持Workflow/Groovy双模式检测
```java
boolean hasWorkflow = skillConfig.containsKey("workflow");
String typeTag = hasWorkflow ? "[Workflow]" : "[Groovy]";
log.info("启用 {} ({})", skillName, typeTag);
```

---

#### (5) 明确generateWithTools模型策略 (P1 - 已完成)
**决策**: 保持使用qwen3推理模型(而非代码模型)

**依据**:
- Ollama官方文档: Tool Calling需要强推理能力理解工具描述
- qwen2.5-coder更适合代码生成,而非工具调度决策
- 业界实践: Claude Code/OpenCode均使用通用推理模型处理Tool Calling

**行动**: `LLMService.java` 添加详细设计决策注释(第174-185行)

---

#### (6) 更新系统文档 (P2 - 已完成)
**修改**: `SYSTEM_INTRODUCTION.md`
- 标记已完成项(统一SQL纠错/单元测试/Workflow试点)
- 调整优化路线优先级
- 补充架构演进说明

---

### 1.3 追问按钮交互优化

**问题**: 点击追问按钮后不消失,历史记录中可重复点击导致混乱。

**需求**: 类似DeepSeek/豆包的交互逻辑
```
查询返回 → [AI总结] [图表] [下载Excel]
点击[AI总结] → 按钮消失 → AI总结返回 → [图表] [下载Excel]
点击[图表] → 按钮消失 → 图表返回 → [下载Excel]
点击[下载Excel] → 按钮消失 → 下载完成 → 无追问
用户输入新问题 → 旧追问消失 → 显示新追问
```

**实现方案**:

1. **每个追问容器生成唯一ID**
```javascript
const containerId = `follow-up-${Date.now()}`;
container.id = containerId;
```

2. **点击任一按钮时删除整个容器**
```javascript
function triggerAISummary(query, containerId) {
    removeFollowUpHints(containerId);  // 立即删除
    sendMessage(`请对以下查询进行AI总结: ${query}`);
}
```

3. **AI返回后重新渲染剩余按钮**
```javascript
if (intent === 'ai_summary') {
    // AI总结完成后,显示剩余按钮
    renderRemainingFollowUpHints(originalQuery, ['chart', 'excel']);
}
```

4. **新查询时清除之前追问状态**
```javascript
function sendMessage(message) {
    // 清除之前的追问状态
    lastQueryForFollowUp = null;
    lastDataForFollowUp = null;
    // ...发送消息逻辑
}
```

**修改文件**: `agent-chat.html`
- `renderDynamicFollowUpHints()` - 添加容器ID
- `triggerAISummary()` / `triggerChartGeneration()` / `triggerExcelDownload()` - 添加删除逻辑
- `handleAgentResponse()` - AI总结/图表返回后重新渲染剩余按钮
- `sendMessage()` - 新查询时清除追问状态
- `removeFollowUpHints()` - 新增删除函数
- `renderRemainingFollowUpHints()` - 新增重新渲染函数

---

### 1.4 Chroma向量维度冲突修复

**问题**: 
```
Chroma语义检索失败: query=最近7天每天的订单总数, 
error=HTTP error: {"error":"InvalidArgumentError",
"message":"Collection expecting embedding with dimension of 384, got 1024"}
```

**根因**: Chroma集合期望384维向量,但bge-m3模型生成1024维。

**解决方案**: 删除旧的Chroma集合,让程序重新创建(自动使用正确维度)。

**实施步骤**:

1. **创建Python脚本** `delete_chroma_collections.py`
```python
import chromadb

client = chromadb.HttpClient(host='localhost', port=8000)
collections_to_delete = ["nl2sql_query_cache"]

for collection_name in collections_to_delete:
    try:
        client.delete_collection(name=collection_name)
        print(f"✅ 成功删除集合: {collection_name}")
    except Exception as e:
        print(f"❌ 删除失败: {e}")
```

2. **执行删除**
```bash
python delete_chroma_collections.py
```

**踩坑记录**:
- ❌ v1 API已废弃(410 Gone): `DELETE /api/v1/collections/{name}`
- ❌ requests直接调用需tenant/database参数
- ✅ 最终使用chromadb Python库成功删除

---

### 1.5 元数据LLM自动增强

**背景**: 向量检索表召回准确率低(orders表相似度仅0.35),根本原因是表描述过于简单("订单主表"),缺乏业务关键词。

**解决方案**: 使用LLM基于完整表结构信息自动生成业务化描述。

#### (1) 表级元数据增强
**文件**: `MetadataCollectorService.java`  
**方法**: `enhanceTableDescriptionsAsync(Long datasourceId)`

**功能**: 
- 读取表的完整字段元数据(类型/长度/主键/非空约束/默认值/注释)
- 构建结构化提示词,包含"表基本信息"和"字段详细信息"
- LLM生成50字以内业务化描述,包含核心业务指标关键词
- 异步执行,不阻塞元数据同步流程

**提示词示例**:
```
=== 表基本信息 ===
表名: orders
当前描述: 订单主表

=== 字段详细信息 ===
- order_id (BIGINT, [主键], 非空): 订单ID
- order_amount (DECIMAL, 长度:10, 非空): 订单金额
- user_id (BIGINT, 非空): 用户ID

=== 生成要求 ===
1. 识别核心业务指标
2. 说明表的业务用途
3. 突出关联关系
4. 包含检索关键词
5. 简洁专业,50字以内
```

**输出示例**: "订单主表,记录订单金额、状态、用户信息,用于分析订单量、销售额及用户行为,关联用户表。"

---

#### (2) 列级元数据增强
**方法**: `enhanceColumnDescriptionsAsync(Long datasourceId)`

**功能**:
- 遍历所有表的字段
- 跳过已有注释的字段(保留手动修改)
- 基于字段名+类型+表上下文推断业务含义
- 生成20字以内简洁中文注释
- 限流保护:每个字段间隔500ms调用LLM

**提示词示例**:
```
表名: orders
表说明: 订单主表,记录订单金额、状态、用户信息
字段名: order_amount
数据类型: DECIMAL
字段长度: 10
约束: 非空

要求:
1. 根据字段名推测业务含义
2. 结合表说明理解业务场景
3. 简洁准确,20字以内
```

**输出示例**: "订单金额"

---

#### (3) 自动触发机制
**元数据同步时**: 
- 立即启动表增强
- 延迟30秒后启动列增强(确保表描述已生成)

**手动触发接口**:
- POST `/api/admin/metadata/enhance/{datasourceId}` - 表增强
- POST `/api/admin/metadata/enhance-columns/{datasourceId}` - 列增强

---

#### (4) Controller接口
**文件**: `AdminMetadataController.java`

新增两个REST接口:
```java
@PostMapping("/metadata/enhance/{datasourceId}")
public Result<Map<String, Object>> enhanceMetadata(@PathVariable Long datasourceId)

@PostMapping("/metadata/enhance-columns/{datasourceId}")
public Result<Map<String, Object>> enhanceColumnMetadata(@PathVariable Long datasourceId)
```

---

## 二、踩坑与解决

### 坑1: MultiModelService方法不存在

**现象**: 编译错误 `找不到符号: 方法 generateWithOllama(String,String,double)`

**根因**: 
- 误以为MultiModelService有直接的Ollama调用方法
- 实际需要通过`getLlmService()`获取LLMService实例

**解决**:
1. 在MultiModelService添加getter方法:
```java
public LLMService getLlmService() {
    return llmService;
}
```
2. 调用方式改为: `multiModelService.getLlmService().generateAnswer(prompt)`

**教训**: 调用第三方服务前先查看实际API,不要假设方法存在。

---

### 坑2: SyncResult缺少datasourceId字段

**现象**: 编译错误 `找不到符号: 方法 setDatasourceId(Long)`

**根因**: 
- enhanceTableDescriptionsAsync最初设计为接收`List<TableMetadata>`参数
- 后来改为只传`datasourceId`,内部查询表列表
- 但SyncResult类缺少datasourceId字段

**解决**: 在SyncResult内部类添加字段:
```java
@Data
public static class SyncResult {
    private Long datasourceId;  // ✅ 新增
    private String status;
    // ...其他字段
}
```

**教训**: 修改方法签名时,检查所有相关类的字段完整性。

---

### 坑3: Maven打包jar文件被占用

**现象**: `Unable to rename 'nl2sql-web-1.0.0.jar' to 'nl2sql-web-1.0.0.jar.original'`

**根因**: 应用仍在运行,jar文件被JVM进程锁定

**解决**:
1. 查找Java进程: `Get-Process | Where-Object {$_.ProcessName -eq "java"}`
2. 强制停止: `Stop-Process -Id <PID> -Force`
3. 重新打包: `mvn clean package "-Dmaven.test.skip=true"`

**教训**: 打包前确保应用已停止,或使用clean先删除旧文件。

---

### 坑4: PowerShell参数解析错误

**现象**: `Unknown lifecycle phase ".test.skip=true"`

**根因**: PowerShell将`-Dmaven.test.skip=true`中的`.`解析为参数分隔符

**解决**: 用引号包裹整个参数: `"-Dmaven.test.skip=true"`

**教训**: PowerShell中Maven参数含特殊字符时需加引号。

---

### 坑5: 列增强依赖表增强的执行顺序

**问题**: 列增强提示词中包含`table_comment`,如果表描述未生成,列推断不准确

**初始方案**: 用户手动先后调用两个接口

**优化方案**: 
- 元数据同步后自动触发表增强
- 延迟30秒后自动触发列增强
- 确保表描述已生成,列增强能利用完整上下文

**代码**:
```java
enhanceTableDescriptionsAsync(datasourceId);
new Thread(() -> {
    Thread.sleep(30000);
    enhanceColumnDescriptionsAsync(datasourceId);
}, "column-enhance-delay-thread").start();
```

**教训**: 有依赖关系的异步任务需要合理安排执行顺序。

---

### 坑6: ReActAgentTest缺少userMessage参数

**现象**: 测试编译失败,构造函数参数不匹配

**根因**: ReActAgent构造函数增加了userMessage参数,但测试用例未同步更新

**解决**: 补充第5个参数
```java
ReActAgent agent = new ReActAgent(llmService, tools, config, sessionId, userMessage);
```

**教训**: 修改代码必须同步更新测试用例。

---

### 坑7: Chroma API版本混淆

**现象**: 
- v1 API返回410 Gone
- v2 API需要tenant/database参数

**根因**: Chroma升级后API路径变更,未查阅官方文档

**解决**: 
1. 搜索Chroma v2 API文档
2. 使用正确的路径: `/api/v2/tenants/default_tenant/databases/default_database/collections/{name}`
3. 改用chromadb Python库简化调用

**教训**: API升级时必须查阅官方文档,不能凭经验猜测。

---

## 三、技术反思

### 3.1 做得好的地方

#### (1) 架构决策清晰
- **Workflow + 原子Tool作为目标架构**: 符合业界最佳实践(LangChain/Airflow/GitHub Actions)
- **Groovy仅作为复杂逻辑兜底**: 80%场景用声明式YAML,20%用代码扩展
- **渐进式重构**: 不破坏现有功能,逐步向目标架构演进

#### (2) 提示词工程优化
- **结构化分段**: 表基本信息 + 字段详细信息 + 生成要求
- **完整上下文**: 传递字段类型/长度/主键/约束/默认值等完整信息
- **明确的生成要求**: 50字以内、包含业务关键词、突出关联关系

#### (3) 用户体验优化
- **追问按钮互斥消失**: 符合DeepSeek/豆包交互习惯
- **智能跳过已有注释**: 保留用户手动修改,避免覆盖
- **异步执行不阻塞**: 元数据同步后立即返回,增强后台执行

---

### 3.2 可改进的地方

#### (1) 测试覆盖率不足
- 当前仅覆盖核心模块(ReActAgent/SQLCorrection/3个Tools)
- 缺少端到端集成测试
- 建议: 补充Workflow执行流程测试

#### (2) 错误处理不够完善
- LLM调用失败时仅记录日志,无重试机制
- 建议: 添加指数退避重试 + 降级策略

#### (3) 性能优化空间
- 列增强逐个字段调用LLM,8个表×平均10字段 = 80次调用 × 500ms = 40秒
- 建议: 批量生成(一次调用生成多个字段注释)

---

### 3.3 架构思考

#### 为什么Workflow + 原子Tool是正确的方向?

**对比分析**:

| 维度 | 纯Groovy架构 | 混合架构(YAML+Groovy) |
|------|-------------|---------------------|
| **可读性** | ❌ 需阅读代码 | ✅ YAML一目了然 |
| **可维护性** | ❌ 分散在各文件 | ✅ 集中管理 |
| **可测试性** | ❌ 需完整容器 | ✅ Tool独立单元测试 |
| **灵活性** | ✅ 图灵完备 | ⚠️ YAML受限,Groovy兜底 |
| **学习成本** | ❌ 需懂Groovy+Spring | ✅ YAML低门槛 |
| **性能** | ✅ 无解析开销 | ⚠️ ~10ms解析开销 |

**结论**: 混合架构在**可维护性**和**灵活性**之间取得最佳平衡。

---

## 四、关键代码片段

### 4.1 表级增强提示词构建
```java
StringBuilder prompt = new StringBuilder();
prompt.append("你是一个数据库专家和业务分析师。请根据以下完整的表结构信息，生成一段简洁的业务化表描述(50字以内)。\n\n");

// 表基本信息
prompt.append("=== 表基本信息 ===\n");
prompt.append("表名: ").append(tableName).append("\n");
prompt.append("当前描述: ").append(currentComment != null ? currentComment : "无").append("\n\n");

// 字段详细信息
prompt.append("=== 字段详细信息 ===\n");
for (Map<String, Object> col : columns) {
    prompt.append(String.format("- %s (%s", col.get("column_name"), col.get("data_type")));
    
    if (col.get("column_size") != null) {
        prompt.append(", 长度:").append(col.get("column_size"));
    }
    if ("1".equals(String.valueOf(col.get("is_primary_key")))) {
        prompt.append(", [主键]");
    }
    if ("0".equals(String.valueOf(col.get("is_nullable")))) {
        prompt.append(", 非空");
    }
    if (col.get("column_default") != null) {
        prompt.append(", 默认:").append(col.get("column_default"));
    }
    
    prompt.append(")");
    
    if (col.get("column_comment") != null && !col.get("column_comment").toString().isEmpty()) {
        prompt.append(": ").append(col.get("column_comment"));
    } else {
        prompt.append(": 无说明");
    }
    prompt.append("\n");
}

// 生成要求
prompt.append("\n=== 生成要求 ===\n");
prompt.append("1. **识别核心业务指标**: 从字段中识别关键业务概念(如订单量、销售额、用户数、库存量等)\n");
prompt.append("2. **说明表的业务用途**: 这张表在业务系统中扮演什么角色\n");
prompt.append("3. **突出关联关系**: 如果有外键或关联字段,说明与其他表的关系\n");
prompt.append("4. **包含检索关键词**: 确保描述包含用户可能查询的业务术语\n");
prompt.append("5. **简洁专业**: 50字以内,便于向量检索匹配\n");
prompt.append("6. **只返回描述文本**: 不要任何解释、前缀或其他内容\n\n");
prompt.append("生成的描述:");
```

---

### 4.2 列级增强智能跳过
```java
for (Map<String, Object> col : columns) {
    String columnName = (String) col.get("column_name");
    String currentComment = (String) col.get("column_comment");
    
    // 如果已有注释且不为空，跳过（保留手动修改）
    if (currentComment != null && !currentComment.trim().isEmpty() 
        && !currentComment.equals("无说明")) {
        log.debug("[列元数据增强] 跳过已有注释的字段: {}.{}", tableName, columnName);
        continue;
    }
    
    // 调用LLM生成注释
    String enhancedComment = callLLM(prompt.toString());
    if (enhancedComment != null && enhancedComment.length() > 2 
        && enhancedComment.length() <= 20) {
        updateColumnComment(datasourceId, tableName, columnName, enhancedComment);
        totalEnhanced++;
    }
    
    // 限流保护
    Thread.sleep(500);
}
```

---

### 4.3 追问按钮删除逻辑
```javascript
// 生成唯一ID
const containerId = `follow-up-${Date.now()}`;
container.id = containerId;

// 点击时删除
function triggerAISummary(query, containerId) {
    removeFollowUpHints(containerId);
    sendMessage(`请对以下查询进行AI总结: ${query}`);
}

// 删除函数
function removeFollowUpHints(containerId) {
    const container = document.getElementById(containerId);
    if (container) {
        container.remove();
    }
}

// 重新渲染剩余按钮
function renderRemainingFollowUpHints(query, remainingTypes) {
    if (remainingTypes.length === 0) return;
    
    const hints = [];
    if (remainingTypes.includes('ai_summary')) {
        hints.push({ type: 'ai_summary', text: '📊 AI总结' });
    }
    if (remainingTypes.includes('chart')) {
        hints.push({ type: 'chart', text: '📈 生成图表' });
    }
    if (remainingTypes.includes('excel')) {
        hints.push({ type: 'excel', text: '📥 下载Excel' });
    }
    
    renderDynamicFollowUpHints(query, hints);
}
```

---

## 五、后续优化方向

### 5.1 短期优化(1-2周)

#### (1) 完善Workflow能力
- 支持循环 (`for_each`)
- 支持子Workflow (`call_workflow`)
- 支持错误处理 (`on_error`)

#### (2) 迁移更多Skills到Workflow
优先迁移:
- `table-structure-query` → 单表查询场景
- `basic-statistics` → 基础统计计算

保留Groovy:
- `standard-query` → 复杂风险评估+自动修正
- `report-with-insights` → 动态报告生成

#### (3) 补充端到端测试
- Workflow执行流程测试
- 追问按钮交互测试
- 元数据增强效果验证

---

### 5.2 中期优化(1-2月)

#### (1) 事件驱动架构
当前实现:
```java
// 硬编码延迟30秒
Thread.sleep(30000);
enhanceColumnDescriptionsAsync(datasourceId);
```

优化方案:
```java
// 表增强完成后发布事件
eventPublisher.publishEvent(new TableEnhanceCompletedEvent(datasourceId));

// 列增强监听事件
@EventListener
public void onTableEnhanceCompleted(TableEnhanceCompletedEvent event) {
    enhanceColumnDescriptionsAsync(event.getDatasourceId());
}
```

#### (2) 增强进度追踪
- 前端显示增强进度条
- 后端记录增强状态(待执行/执行中/已完成/失败)
- 提供查询接口: GET `/api/admin/metadata/enhance-status/{datasourceId}`

#### (3) 失败重试机制
- LLM调用失败时指数退避重试(最多3次)
- 记录失败原因到日志
- 提供手动重试接口

---

### 5.3 长期优化(3-6月)

#### (1) 增量增强
当前: 每次同步全量增强所有表  
优化: 仅增强新增/修改的表

```java
// 检测表结构变化
List<String> changedTables = detectSchemaChanges(datasourceId);
changedTables.forEach(this::enhanceTableDescription);
```

#### (2) 向量索引热加载
当前: 增强后需重启应用  
优化: 动态重新加载向量索引

```java
// 增强完成后触发重新索引
vectorRetriever.reloadIndex(datasourceId);
```

#### (3) 增强质量评估
- A/B测试: 对比增强前后的向量检索准确率
- 用户反馈: 记录查询成功率提升
- 自动化评分: LLM评估生成描述的质量

---

## 六、总结

### 6.1 今日完成的工作量

**代码改动**:
- 新增文件: 12个(3个Tools + 6个测试 + 2个脚本 + 1个Workflow配置)
- 修改文件: 7个(AgentConfig/LLMService/MetadataCollectorService/AdminMetadataController/StandardQuerySkill/agent-chat.html/SYSTEM_INTRODUCTION.md)
- 总行数: +1200/-50 (净增约1150行)

**测试覆盖**:
- 单元测试: 28个用例(ReActAgent 7个 + SQLCorrection 8个 + 3个Tools 13个)
- 通过率: 100%

**文档产出**:
- 系统架构文档更新
- Workflow迁移标准
- 本复盘文档

---

### 6.2 核心价值

1. **架构演进**: 从"脚本驱动"向"配置驱动"迈进,建立Workflow + 原子Tool的目标架构
2. **体验优化**: 追问按钮交互符合主流AI产品习惯,降低用户认知成本
3. **检索提升**: 元数据自动增强预计可将向量检索准确率从0.35提升至0.7+
4. **可维护性**: 统一SQL纠错机制消除代码重复,集中管理降低维护成本

---

### 6.3 经验教训

1. **调用API前先确认方法签名**: MultiModelService踩坑
2. **修改代码必须同步更新测试**: ReActAgentTest参数缺失
3. **有依赖的异步任务需合理安排顺序**: 表增强先于列增强
4. **API升级时必须查阅官方文档**: Chroma v1→v2迁移
5. **PowerShell特殊字符需转义**: Maven参数加引号
6. **打包前确保应用已停止**: jar文件占用问题

---

### 6.4 明日计划

1. 重启应用验证所有改动
2. 观察元数据增强效果(向量检索准确率)
3. 测试Workflow试点Skill执行情况
4. 如有问题及时调整

---

**文档编写时间**: 2026-04-22 晚上  
**会话时长**: 约10小时 (07:49 - 18:00)  
**提交Commit**: `feat: add metadata auto-enhancement with LLM`
